{
  "name": "chat-backend",
  "version": "1.0.0",
  "main": "src/app.ts",
  "private": true,
  "scripts": {
    "start:build": "tsc --watch",
    "start:dev": "node --watch dist/app.js",
    "dev": "concurrently --success 'first' --raw npm:start:build npm:start:dev",
    "build": "tsc",
    "start": "node dist/app.js"
  },
  "author": "al-imam",
  "dependencies": {
    "@types/bcrypt": "^5.0.0",
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.11",
    "concurrently": "^8.0.1",
    "typescript": "^5.0.4"
  }
}
